writer        = rolling file
writer.format = {date} {level}: {message}
writer.level  = debug
# Add PID for safety
#writer.file   = log_{pid}_{count}.txt
#writer.latest   = latest_{pid}.log
writer.file   = log_{count}.txt
writer.latest   = latest.log
writer.policies = size: 10mb
